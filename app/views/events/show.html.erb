<% @title ||= ti(:title, model: full_entry_label).html_safe -%>

<% content_for(:actions, render('actions_show')) %>

<%= render 'attrs' %>
<%= image_tag "http://maps.google.com/maps/api/staticmap?size=450x300&sensor=false&zoom=10&markers=#{@event.host.latitude}%2C#{@event.host.longitude}" %>

